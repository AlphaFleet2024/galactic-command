import React from 'react';
import ShipList from './ShipList';

const Dashboard = () => {
    return (
        <div>
            <h1>Your Dashboard</h1>
            <ShipList />
        </div>
    );
};

export default Dashboard;
